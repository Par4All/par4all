#
# $RCSfile: makefile_macros.GNUSOL1,v $ (version $Revision$)
# $Date: 1996/07/15 14:44:56 $, 
#
# this file contains macro definitions 

AR	= gar
ARFLAGS	= rv
CC	= gcc
CFLAGS	= -O2 -g -ansi -pedantic -Wall -mflat -msupersparc -pipe
CPPFLAGS= -D__USE_FIXED_PROTOTYPES__ -I$(NEWGEN_INCLUDEDIR) -I$(LINEAR_INCLUDEDIR) -I$(PIPS_INCLUDEDIR) -I$(PIPS_EXTEDIR)
LD	= $(CC) 
LDFLAGS	= -g -L./$(ARCH) -L$(PIPS_LIBDIR)/$(ARCH) -L$(NEWGEN_LIBDIR)/$(ARCH) -L$(LINEAR_LIBDIR)/$(ARCH) -L$(PIPS_EXTEDIR)/$(ARCH)
RANLIB	= granlib
LEX	= lex
LFLAGS	=
FC	= g77
FFLAGS	= -O2 -g -Wimplicit -pipe
LINT	= lint
LINTFLAGS= -habxu
YACC	= yacc
YFLAGS	=
PROTOIZE= cproto -vcf2
ETAGS	= etags
TAR	= gtar
ZIP	= gzip -v9
DIFF	= diff
M4	= gm4

# end of $RCSfile: makefile_macros.GNUSOL1,v $
#
