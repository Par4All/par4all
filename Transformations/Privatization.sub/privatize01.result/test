#
# Code after privatize_module for module papy_boom
#
void papy_boom()
{
   int i = 0;
#pragma omp for private(i)
   for(i = 0; i <= 9; i += 1)
      printf("%i", i);
}
#
# Code after localize_declaration for module papy_boom
#
void papy_boom()
{
   int i = 0;
#pragma omp for private(i)
   for(i = 0; i <= 9; i += 1)
      printf("%i", i);
}
