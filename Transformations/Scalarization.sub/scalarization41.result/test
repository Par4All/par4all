void out0()
{
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      a[j] = 1;
      for(k = j; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[k];
   }
   printf("%d", b[0]);
}
void out1()
{
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      a[j] = 1;
      for(k = 0; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[j];
   }
   printf("%d", b[0]);
}
void out0()
{
   {
      //PIPS generated variable
      register int b_0;

      b_0 = 0;
   }
   for(j = 0; j <= 9; j += 1) {
      a[j] = 1;
      for(k = j; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[k];
   }
   printf("%d", b[0]);
}
void out1()
{
   {
      //PIPS generated variable
      register int b_0;

      b_0 = 0;
   }
   for(j = 0; j <= 9; j += 1) {
      //PIPS generated variable
      register int a_0;
      a_0 = 1;
      for(k = 0; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a_0;
   }
   printf("%d", b[0]);
}
