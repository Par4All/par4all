void out0()
{
   // BEGIN BLOCK
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      // BEGIN BLOCK
      a[j] = 1;
      for(k = j; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[k];
      // END BLOCK
   }
   printf("%d", b[0]);
   // END BLOCK
}
void out1()
{
   // BEGIN BLOCK
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      // BEGIN BLOCK
      a[j] = 1;
      for(k = 0; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[j];
      // END BLOCK
   }
   printf("%d", b[0]);
   // END BLOCK
}
void out0()
{
   // BEGIN BLOCK
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      // BEGIN BLOCK
      a[j] = 1;
      for(k = j; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = a[k];
      // END BLOCK
   }
   printf("%d", b[0]);
   // END BLOCK
}
void out1()
{
   // BEGIN BLOCK
   //PIPS generated variable
   int __scalar__0;
   b[0] = 0;
   for(j = 0; j <= 9; j += 1) {
      // BEGIN BLOCK
      __scalar__0 = 1;
      for(k = 0; k <= 9; k += 1)
         // There should be an out region out here !
         b[k] = __scalar__0;
      // END BLOCK
   }
   printf("%d", b[0]);
   // END BLOCK
}
