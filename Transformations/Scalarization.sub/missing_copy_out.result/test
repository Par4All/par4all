//
// Initial code for module scalarization2
//
void scalarization2()
{
   b[0] = 0;
   for(j = 0; j <= 0; j += 1) {
      a[j] = 1;
      for(k = 0; k <= 0; k += 1)
         b[j] = a[j];
   }
   printf("%d", b[0]);
}
//
// Initial code for module main
//
int main()
{
   scalarization1();
   scalarization2();
   return 0;
}
//
// Initial code for module scalarization1
//
void scalarization1()
{

   b[0] = 0;
   for(j = 0; j <= 0; j += 1) {
      a[j] = 1;
      for(k = 0; k <= 0; k += 1)
         b[j] = a[j];
   }
   printf("%d", b[0]);
   
   /* This printf used to prevent copy out for b[] when scalarized */
   printf("%s", name);
}
// Execution after scalarization
11
//
// Code after scalarization for module scalarization2
//
void scalarization2()
{
   //PIPS generated variable
   int __scalar__0, __scalar__1;
   b[0] = 0;
   for(j = 0; j <= 0; j += 1) {
      __scalar__0 = b[j];
      __scalar__1 = 1;
      for(k = 0; k <= 0; k += 1)
         __scalar__0 = __scalar__1;
      b[j] = __scalar__0;
   }
   printf("%d", b[0]);
}
// Execution after scalarization
11
//
// Code after scalarization for module main
//
int main()
{
   scalarization1();
   scalarization2();
   return 0;
}
// Execution after scalarization
01
//
// Code after scalarization for module scalarization1
//
void scalarization1()
{
   //PIPS generated variable
   int __scalar__0, __scalar__1;

   b[0] = 0;
   for(j = 0; j <= 0; j += 1) {
      __scalar__0 = b[j];
      __scalar__1 = 1;
      for(k = 0; k <= 0; k += 1)
         __scalar__0 = __scalar__1;
   }
   printf("%d", b[0]);
   
   /* This printf used to prevent copy out for b[] when scalarized */
   printf("%s", name);
}
