
Code after scalarization

void scalarization10(double A[SIZE], double B[SIZE][SIZE])
{
   // BEGIN BLOCK
   double __ld__0;
   int i;
   int j;
   for(i = 0; i <= SIZE-1; i += 1) {
      // BEGIN BLOCK
      __ld__0 = A[i];
      for(j = 0; j <= SIZE-1; j += 1)
         __ld__0 = B[j][i]+get(__ld__0, i);
      // END BLOCK
   }
   // END BLOCK
}
