
Initial code

int main(int argc, char **argv)
{
   // BEGIN BLOCK
   int i;
   int n = 100;
   int x[n];
   int y[n];
   int t[n];

   for(i = 0; i <= n-1; i += 1)
      scanf("%d %d", &x[i], &y[i]);

   for(i = 0; i <= n-1; i += 1) {
      // BEGIN BLOCK
      t[i] = x[i];
      x[i] = y[i];
      y[i] = t[i];
      // END BLOCK
   }
   printf("%d %d", x[n], y[n]);
   // END BLOCK
}

Code after scalarization

int main(int argc, char **argv)
{
   // BEGIN BLOCK
   int __ld__0;
   int i;
   int n = 100;
   int x[n];
   int y[n];
   int t[n];

   for(i = 0; i <= n-1; i += 1)
      scanf("%d %d", &x[i], &y[i]);

   for(i = 0; i <= n-1; i += 1) {
      // BEGIN BLOCK
      __ld__0 = x[i];
      x[i] = y[i];
      y[i] = __ld__0;
      // END BLOCK
   }
   printf("%d %d", x[n], y[n]);
   // END BLOCK
}
