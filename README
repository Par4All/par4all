PIPS validation, i.e. non regression tests.


Default validation or just for one directory:

	sh> make validate
	sh> make TARGET=Semantics validate


Interactive acceptation of differences, and commit:

	sh> make accept
	sh> svn ci


Full cleanup of generated files:

	sh> make clean
