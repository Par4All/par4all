
Parsed printed file for "decl38"

void decl38(int size)
{
// (statement ordering unavailable)
   // BEGIN BLOCK
   int i;
   int j;
   int k;
   float x;
// (statement ordering unavailable)

   for (i = 1;(i<size);i++)                                             /*0009*/
// (statement ordering unavailable)
      ;
// (statement ordering unavailable)
   // BEGIN BLOCK
// (statement ordering unavailable)
   
   
   // if the switch is removed, the loop index declarations reappears
   ;                                                                    /*0013*/
// (statement ordering unavailable)
   // BEGIN BLOCK
// (statement ordering unavailable)
   if ((0==0))
// (statement ordering unavailable)
      goto switch_2_case_0;                                             /*0014*/
   else
// (statement ordering unavailable)
      ;
// (statement ordering unavailable)
   goto break_2;                                                        /*0017*/
   // END BLOCK
// (statement ordering unavailable)
switch_2_case_0:   ;
// (statement ordering unavailable)
   for (j = 1;(j<size);j++)                                             /*0015*/
// (statement ordering unavailable)
      ;
// (statement ordering unavailable)
break_2:   ;
   // END BLOCK
// (statement ordering unavailable)

   for (k = 1;(k<size);k++)                                             /*0019*/
// (statement ordering unavailable)
      ;
// (statement ordering unavailable)

   x = 0;                                                               /*0021*/
   // END BLOCK
}

Printed file for "decl38"

void decl38(int size)
{
// (0,1)
   int i;
   int j;
   int k;
   float x;
// (1,1)

   for(i = 1; i <= (size-1); i += 1)                                    /*0009*/
// (1,2)
      ;
// (2,1)


// if the switch is removed, the loop index declarations reappears   if ((0==0)) goto l99999;
// (9,1)
   ;
// (8,1)
   ;
// (7,1)
   // BEGIN BLOCK
// (7,2)
   ;
// (7,3)
   ;                                                                    /*0017*/
   // END BLOCK
// (5,1)
   // BEGIN BLOCK
// (5,2)
break_2:   ;
// (5,3)

   for(k = 1; k <= (size-1); k += 1)                                    /*0019*/
// (5,4)
      ;
// (5,5)

   x = 0;                                                               /*0021*/
   // END BLOCK
   goto l99998;
l99999:   ;
// (3,1)
   ;                                                                    /*0014*/
// (4,1)
   // BEGIN BLOCK
// (4,2)
   ;
// (4,3)
switch_2_case_0:   ;
// (4,4)
   for(j = 1; j <= (size-1); j += 1)                                    /*0015*/
// (4,5)
      ;
   // END BLOCK
   goto break_2;
l99998:   ;
// (6,1)
   ;
}
