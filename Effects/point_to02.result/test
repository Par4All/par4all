
Parsed printed file for "point_to02"

void point_to02()
{
   struct s {
      int a;
      int b[10];
   };
   struct s c;
   struct s *p;

   p->a = 1;
   (p->b)[2] = 3;
}

Cumulated effetcs for "point_to02"

void point_to02()
{
   struct s {
      int a;
      int b[10];
   };
   struct s c;
   struct s *p;
//               <must be written - post>:

   p->a = 1;
//               <may be written >: TOP-LEVEL:*MEMORY*
   (p->b)[2] = 3;
}
