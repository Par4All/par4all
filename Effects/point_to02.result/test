
Parsed printed file for "point_to02"

void point_to02()
{
   struct s {
      int a;
      int b[10];
   };
   struct s c;
   struct s *p;

   p->a = 1;
   (p->b)[2] = 3;
}

Cumulated effetcs for "point_to02"

//               <may be written >: TOP-LEVEL:*MEMORY*
void point_to02()
{
   struct s {
      int a;
      int b[10];
   };
   struct s c;
   struct s *p;
//               <must be read   >: p
//               <must be written - post>: (*p)[1]

   p->a = 1;
//               <must be read   >: p
//               <must be written - post>: (*p)[2][2]
   (p->b)[2] = 3;
}
