
Parsed printed file for "assign02"

void assign02()
{
   s_t s;
   int j;

   s = make_s(1, 2.);
   s.entier = 4;
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}

Proper pointer effects for "make_s"

s_t make_s(int i, float x)
{
   s_t *p;
   char *malloc(int);
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ p

   p = (s_t *) malloc(sizeof(s_t));
//             <    is read   >: i p
//             <    is written>: p[0].entier
   p->entier = i;
//             <    is read   >: p x
//             <    is written>: p[0].reel
   p->reel = x;
//             <    is read   >: p p[0].reel p[0].entier
   return *p;
}

Cumulated pointer effects for "make_s"

//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_ i x
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_
s_t make_s(int i, float x)
{
   s_t *p;
   char *malloc(int);
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ p

   p = (s_t *) malloc(sizeof(s_t));
//             <    is read   >: i p
//             <    is written>: p[0].entier
   p->entier = i;
//             <    is read   >: p x
//             <    is written>: p[0].reel
   p->reel = x;
//             <    is read   >: p p[0].reel p[0].entier
   return *p;
}

Proper pointer effects for "assign02"

void assign02()
{
   s_t s;
   int j;
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ s.entier 
//                               s.reel

   s = make_s(1, 2.);
//             <    is written>: s.entier
   s.entier = 4;
//             <may be read   >: *ANY_MODULE*:*ANYWHERE*
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ j
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}

Cumulated pointer effects for "assign02"

//             <may be read   >: *ANY_MODULE*:*ANYWHERE*
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_
void assign02()
{
   s_t s;
   int j;
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ s.entier 
//                               s.reel

   s = make_s(1, 2.);
//             <    is written>: s.entier
   s.entier = 4;
//             <may be read   >: *ANY_MODULE*:*ANYWHERE*
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_ j
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}
