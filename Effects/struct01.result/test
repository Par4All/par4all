
Cumulated effetcs for "struct01"

int struct01()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   int y[10];
//               <may be written >: x[*]

   x.first = 1;
//               <may be written >: x[*]
   x.second = 2;
//               <may be written >: y[*]

   y[0] = 1;
}

Transformers for "struct01"


//  T() {}

int struct01()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   int y[10];

//  T() {}


   x.first = 1;

//  T() {}

   x.second = 2;

//  T() {}


   y[0] = 1;
}

Printed files with regions

int struct01()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   int y[10];

//  <x[PHI1]-W-EXACT-{PHI1==1}>


   x.first = 1;

//  <x[PHI1]-W-EXACT-{PHI1==2}>

   x.second = 2;

//  <y[PHI1]-W-EXACT-{PHI1==0}>


   y[0] = 1;
}
