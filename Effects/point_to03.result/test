
Parsed printed file for "point_to03"

void point_to03()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   struct one *p;

   p = &x;
   p->first = 1;
}

Proper effetcs for "point_to03"

void point_to03()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   struct one *p;
//               <must be written>: p

   p = &x;
//               <must be written - post>:
   p->first = 1;
}

Cumulated effetcs for "point_to03"

void point_to03()
{
   struct one {
      int first;
      int second;
   };
   struct one x;
   struct one *p;
//               <must be written>: p

   p = &x;
//               <must be written - post>:
   p->first = 1;
}
