
Parsed printed file for generate 07

void generate07()
{
   int i = 0;
   double x = 1.;
   foo y;
   typedef union {int either; double or;} z_t;
   z_t z;
   morning_t u;
   extern fii func(foo, z_t, morning_t, double *);
   
   // use an undeclared function without source code, which returns
   // implictly an inta typedef type
   (void) func(y, z, u, &x);
}

Parsed printed file for func

fii func(foo f1, z_t f2, morning_t f3, double *f4)
{
}

Printed synthesized file "func!"

typedef double fii;
enum count {zero, un, deux, trois, quatre};
enum us_count {one=un, two, three, four};
enum kount {null, eins, zwei, drei, vier};
typedef struct {int bonjour:two+drei; int hello:three;} morning_t;
typedef union {int either; double or;} z_t;
typedef fii foo[deux];
fii func(foo, z_t, morning_t, double *);

Compile the generated code

//
//     This module was automatically generated by PIPS
//
typedef double fii;
enum count {zero, un, deux, trois, quatre};
enum us_count {one=un, two, three, four};
enum kount {null, eins, zwei, drei, vier};
typedef struct {int bonjour:two+drei; int hello:three;} morning_t;
typedef union {int either; double or;} z_t;
typedef fii foo[deux];
fii func(foo, z_t, morning_t, double *);
//
//     This module was automatically generated by PIPS and should
//     be updated by the user with READ and WRITE effects on
//     formal parameters to be useful...
//
fii func(f1, f2, f3, f4)
foo f1;
z_t f2;
morning_t f3;
double * f4;
{}
