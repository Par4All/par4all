/* To debug the conflict between Serge's update for register keyword
   and Francois' update for a difference between a type declaration
   "struct s{}"
   and a type reference "struct s"*/

struct s
{
  int a;
};
