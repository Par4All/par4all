
PARSED PRINTED FILE FOR MODULE resp05!

extern int convold1(int X[13][13], int coeff[6], int i, int j);
extern int convold2(int H[8][13], int coeff[6], int i, int j);
extern int filtre(int H[8][13], int K[8][8], int i, int j);
extern void init_coeff(int COEFF[6]);
extern int main();
extern void init_coeff(int COEFF[6]);
extern int convold1(int X[13][13], int coeff[6], int i, int j);
extern int convold2(int H[8][13], int coeff[6], int i, int j);
extern int filtre(int H[8][13], int K[8][8], int i, int j);

PRINTED FILE FOR main

int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;

   init_coeff(COEFF);

   for(i = 0; i <= 12; i += 1)
      for(j = 0; j <= 12; j += 1)
         X[i][j] = j*(j+i)/2;

   for(i = 0; i <= 7; i += 1)
      for(j = 0; j <= 12; j += 1)
         H[i][j] = convold1(X, COEFF, i, j);

   for(i = 0; i <= 7; i += 1)
      for(j = 0; j <= 7; j += 1)
         K[i][j] = convold2(H, COEFF, i, j);

   for(i = 0; i <= 7; i += 1)
      for(j = 0; j <= 7; j += 1)
         Y[i][j] = filtre(H, K, i, j);

   for(i = 0; i <= 7; i += 1)
      for(j = 0; j <= 7; j += 1)
         OUT[i][j] = Y[i][j];
}
int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;
//               <must be read   >: 0`COEFF

   init_coeff(COEFF);
//               <must be written>: 0`i

   for(i = 0; i <= 12; i += 1)
//               <must be written>: 0`j
      for(j = 0; j <= 12; j += 1)
//               <must be read   >: 0`i 0`j
//               <must be written>: 0`X[i][j]
         X[i][j] = j*(j+i)/2;
//               <must be written>: 0`i

   for(i = 0; i <= 7; i += 1)
//               <must be written>: 0`j
      for(j = 0; j <= 12; j += 1)
//               <must be read   >: 0`COEFF 0`X 0`i 0`j
//               <must be written>: 0`H[i][j]
         H[i][j] = convold1(X, COEFF, i, j);
//               <must be written>: 0`i

   for(i = 0; i <= 7; i += 1)
//               <must be written>: 0`j
      for(j = 0; j <= 7; j += 1)
//               <must be read   >: 0`COEFF 0`H 0`i 0`j
//               <must be written>: 0`K[i][j]
         K[i][j] = convold2(H, COEFF, i, j);
//               <must be written>: 0`i

   for(i = 0; i <= 7; i += 1)
//               <must be written>: 0`j
      for(j = 0; j <= 7; j += 1)
//               <must be read   >: 0`H 0`K 0`i 0`j
//               <must be written>: 0`Y[i][j]
         Y[i][j] = filtre(H, K, i, j);
//               <must be written>: 0`i

   for(i = 0; i <= 7; i += 1)
//               <must be written>: 0`j
      for(j = 0; j <= 7; j += 1)
//               <must be read   >: 0`Y[i][j] 0`i 0`j
//               <must be written>: 0`OUT[i][j]
         OUT[i][j] = Y[i][j];
}

//  T() {}

int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;

//  T() {}


   init_coeff(COEFF);

//  T(0`i,0`j) {0<=0`i}


   for(i = 0; i <= 12; i += 1)

//  T(0`j) {0<=0`i, 0`i<=12, 0<=0`j}

      for(j = 0; j <= 12; j += 1)

//  T() {0<=0`i, 0`i<=12, 0<=0`j, 0`j<=12}

         X[i][j] = j*(j+i)/2;

//  T(0`i,0`j) {j#init==13, 0<=0`i}


   for(i = 0; i <= 7; i += 1)

//  T(0`j) {0<=0`i, 0`i<=7, 0<=0`j}

      for(j = 0; j <= 12; j += 1)

//  T() {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=12}

         H[i][j] = convold1(X, COEFF, i, j);

//  T(0`i,0`j) {j#init==13, 0<=0`i}


   for(i = 0; i <= 7; i += 1)

//  T(0`j) {0<=0`i, 0`i<=7, 0<=0`j}

      for(j = 0; j <= 7; j += 1)

//  T() {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         K[i][j] = convold2(H, COEFF, i, j);

//  T(0`i,0`j) {j#init==8, 0<=0`i}


   for(i = 0; i <= 7; i += 1)

//  T(0`j) {0<=0`i, 0`i<=7, 0<=0`j}

      for(j = 0; j <= 7; j += 1)

//  T() {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         Y[i][j] = filtre(H, K, i, j);

//  T(0`i,0`j) {j#init==8, 0<=0`i}


   for(i = 0; i <= 7; i += 1)

//  T(0`j) {0<=0`i, 0`i<=7, 0<=0`j}

      for(j = 0; j <= 7; j += 1)

//  T() {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         OUT[i][j] = Y[i][j];
}

//  P() {}

int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;

//  P() {}


   init_coeff(COEFF);

//  P() {}


   for(i = 0; i <= 12; i += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=12}

      for(j = 0; j <= 12; j += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=12, 0<=0`j, 0`j<=12}

         X[i][j] = j*(j+i)/2;

//  P(0`i,0`j) {0`i==13, 0`j==13}


   for(i = 0; i <= 7; i += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7}

      for(j = 0; j <= 12; j += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=12}

         H[i][j] = convold1(X, COEFF, i, j);

//  P(0`i,0`j) {0`i==8, 0`j==13}


   for(i = 0; i <= 7; i += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7}

      for(j = 0; j <= 7; j += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         K[i][j] = convold2(H, COEFF, i, j);

//  P(0`i,0`j) {0`i==8, 0`j==8}


   for(i = 0; i <= 7; i += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7}

      for(j = 0; j <= 7; j += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         Y[i][j] = filtre(H, K, i, j);

//  P(0`i,0`j) {0`i==8, 0`j==8}


   for(i = 0; i <= 7; i += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7}

      for(j = 0; j <= 7; j += 1)

//  P(0`i,0`j) {0<=0`i, 0`i<=7, 0<=0`j, 0`j<=7}

         OUT[i][j] = Y[i][j];
}
int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;

//  <0`COEFF[PHI1]-W-EXACT-{0<=PHI1, PHI1<=5}>


   init_coeff(COEFF);

//  <0`X[PHI1][PHI2]-W-EXACT-{0<=PHI1, PHI1<=12, 0<=PHI2, PHI2<=12}>


   for(i = 0; i <= 12; i += 1)

//  <0`X[PHI1][PHI2]-W-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=12, 0<=0`i,
//    0`i<=12}>

      for(j = 0; j <= 12; j += 1)

//  <0`X[PHI1][PHI2]-W-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=12,
//    0<=0`j, 0`j<=12}>

         X[i][j] = j*(j+i)/2;

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0`j==13}>
//  <0`H[PHI1][PHI2]-W-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=12,
//    0`j==13}>
//  <0`X[PHI1][PHI2]-R-EXACT-{0<=PHI1, PHI1<=12, 0<=PHI2, PHI2<=12,
//    0`j==13}>


   for(i = 0; i <= 7; i += 1)

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0<=0`i, 0`i<=7}>
//  <0`H[PHI1][PHI2]-W-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=12, 0<=0`i,
//    0`i<=7}>
//  <0`X[PHI1][PHI2]-R-EXACT-{0`i<=PHI1, 0<=PHI1, PHI1<=5+0`i,
//    PHI1<=12, 0<=PHI2, PHI2<=12, 0<=0`i, 0`i<=7}>

      for(j = 0; j <= 12; j += 1)

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0<=0`i, 0`i<=7, 0<=0`j,
//    0`j<=12}>
//  <0`H[PHI1][PHI2]-W-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=7,
//    0<=0`j, 0`j<=12}>
//  <0`X[PHI1][PHI2]-R-EXACT-{0`j==PHI2, 0`i<=PHI1, 0<=PHI1,
//    PHI1<=5+0`i, PHI1<=12, 0<=PHI2, PHI2<=12, 0<=0`i, 0`i<=7}>

         H[i][j] = convold1(X, COEFF, i, j);

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0`j==13}>
//  <0`H[PHI1][PHI2]-R-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=12,
//    0`j==13}>
//  <0`K[PHI1][PHI2]-W-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=7,
//    0`j==13}>


   for(i = 0; i <= 7; i += 1)

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0<=0`i, 0`i<=7}>
//  <0`H[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0<=PHI1, PHI1<=7, 0<=PHI2,
//    PHI2<=12}>
//  <0`K[PHI1][PHI2]-W-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=7, 0<=0`i,
//    0`i<=7}>

      for(j = 0; j <= 7; j += 1)

//  <0`COEFF[PHI1]-R-EXACT-{0<=PHI1, PHI1<=5, 0<=0`i, 0`i<=7, 0<=0`j,
//    0`j<=7}>
//  <0`H[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0<=PHI1, PHI1<=7, 0`j<=PHI2,
//    0<=PHI2, PHI2<=5+0`j, PHI2<=12, 0<=0`j, 0`j<=7}>
//  <0`K[PHI1][PHI2]-W-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=7,
//    0<=0`j, 0`j<=7}>

         K[i][j] = convold2(H, COEFF, i, j);

//  <0`H[PHI1][PHI2]-R-EXACT-{0<=PHI1, PHI1<=7, 2<=PHI2, PHI2<=9,
//    0`j==8}>
//  <0`K[PHI1][PHI2]-R-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=7,
//    0`j==8}>
//  <0`Y[PHI1][PHI2]-W-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=7,
//    0`j==8}>


   for(i = 0; i <= 7; i += 1)

//  <0`H[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0<=PHI1, PHI1<=7, 2<=PHI2,
//    PHI2<=9}>
//  <0`K[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0<=PHI1, PHI1<=7, 0<=PHI2,
//    PHI2<=7}>
//  <0`Y[PHI1][PHI2]-W-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=7, 0<=0`i,
//    0`i<=7}>

      for(j = 0; j <= 7; j += 1)

//  <0`H[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0`j==PHI2-2, 0<=PHI1,
//    PHI1<=7, 0<=0`j, 0`j<=7}>
//  <0`K[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0`j==PHI2, 0<=PHI1, PHI1<=7,
//    0<=PHI2, PHI2<=7}>
//  <0`Y[PHI1][PHI2]-W-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=7,
//    0<=0`j, 0`j<=7}>

         Y[i][j] = filtre(H, K, i, j);

//  <0`OUT[PHI1][PHI2]-W-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=7,
//    0`j==8}>
//  <0`Y[PHI1][PHI2]-R-EXACT-{0<=PHI1, PHI1<=7, 0<=PHI2, PHI2<=7,
//    0`j==8}>


   for(i = 0; i <= 7; i += 1)

//  <0`OUT[PHI1][PHI2]-W-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=7, 0<=0`i,
//    0`i<=7}>
//  <0`Y[PHI1][PHI2]-R-EXACT-{PHI1==0`i, 0<=PHI2, PHI2<=7, 0<=0`i,
//    0`i<=7}>

      for(j = 0; j <= 7; j += 1)

//  <0`OUT[PHI1][PHI2]-W-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=7,
//    0<=0`j, 0`j<=7}>
//  <0`Y[PHI1][PHI2]-R-EXACT-{PHI1==0`i, PHI2==0`j, 0<=0`i, 0`i<=7,
//    0<=0`j, 0`j<=7}>

         OUT[i][j] = Y[i][j];
}
C                                                                18546 (SUMMARY)
int main()
{
   int X[13][13];
   int Y[8][8];
   int H[8][13];
   int K[8][8];
   int OUT[8][8];
   int COEFF[6];
   int i;
   int j;
//                                                                    8 (STMT)

   init_coeff(COEFF);
//                                                                 1886 (DO)

   for(i = 0; i <= 12; i += 1)
//                                                                  145 (DO)
      for(j = 0; j <= 12; j += 1)
//                                                                   11 (STMT)
         X[i][j] = j*(j+i)/2;
//                                                                 8857 (DO)

   for(i = 0; i <= 7; i += 1)
//                                                                 1107 (DO)
      for(j = 0; j <= 12; j += 1)
//                                                                   85 (STMT)
         H[i][j] = convold1(X, COEFF, i, j);
//                                                                 5457 (DO)

   for(i = 0; i <= 7; i += 1)
//                                                                  682 (DO)
      for(j = 0; j <= 7; j += 1)
//                                                                   85 (STMT)
         K[i][j] = convold2(H, COEFF, i, j);
//                                                                 1745 (DO)

   for(i = 0; i <= 7; i += 1)
//                                                                  218 (DO)
      for(j = 0; j <= 7; j += 1)
//                                                                   27 (STMT)
         Y[i][j] = filtre(H, K, i, j);
//                                                                  593 (DO)

   for(i = 0; i <= 7; i += 1)
//                                                                   74 (DO)
      for(j = 0; j <= 7; j += 1)
//                                                                    9 (STMT)
         OUT[i][j] = Y[i][j];
}
