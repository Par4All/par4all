
PARSED PRINTED FILE

void decl01(char *dir)
{
   struct c *y;
   struct c {
      int foo;
      double bar;
   };
   struct c x;
   x.foo = 1;
}

PRINTED_FILE

void decl01(char *dir)
{
   struct c *y;
   struct c {
      int foo;
      double bar;
   };
   struct c x;
   x.foo = 1;
}
