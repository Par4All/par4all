
Parser output

int varargs02(FILE *stream, const char *fmt, ...)
{
   va_list pa;
   int r;
   if (stream) {
      va_list pa;
      int r;

      va_start(pa,fmt);
      r = va_arg(pa, int);
      va_end(pa);
      return r;
   }
   else
      return 0;
}
