
POINTS-TO

//  {}
int main()
{
//  points to = {}
   tf_t s;
//  points to = {}
   tf_t *sp = &s;
//  {(sp,s,-Exact-)}

   call09(s.one, sp->one);
}

PROPER  EFFECTS WITH POINTS TO

int main()
{
   tf_t s;
//             <    is written>: sp
   tf_t *sp = &s;
//             <    is read   >: s.one sp

   call09(s.one, sp->one);
}
