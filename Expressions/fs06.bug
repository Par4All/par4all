forward substitution has no effect when property MEMORY_EFFECTS_ONLY
is set to FALSE.

MEMORY_EFFECTS_ONLY property is needed by DEAD_CODE_ELIMINATION for C
code.

"after forward substitution"
int main(void)
{
   int x, y, z;
   x = 1;
   y = z+x;             // should be y = z+1
   printf("%d\n", y);   // should be printf("%d\n", z+1);
   return 0;
}
