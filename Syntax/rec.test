#!/bin/sh

# Generate an error if a self-recursive call is found

tpips=${tpips_version:-tpips}
PIPS_VALIDDIR=${PIPS_VALIDDIR:-$PIPS_ROOT/Validation}

($tpips 2>&1 | sed '/file *\//d') <<EOF
delete rec
create rec $PIPS_VALIDDIR/Syntax/rec.f
display PRINTED_FILE 
close
delete rec
quit
EOF

