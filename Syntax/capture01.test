#!/bin/sh

# Type check the max operators

tpips=${tpips_version:-tpips}

# ($tpips 2>&1 | grep "user warning") <<EOF
($tpips 2>&1 | sed '/file *\//d') <<EOF
delete capture01
create capture01 ${PIPS_ROOT}/Validation/Syntax/capture01.f
module CAPTURE01
display PRINTED_FILE[CAPTURE01]
close
delete capture01
quit
EOF

