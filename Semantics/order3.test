#!/bin/sh

tpips=${tpips_version:-tpips}

# Initialization:
# rm -f order3.result/test
# tpips >order3.result/test <<EOF

$tpips <<EOF
# Check that preconditions are properly ordered

setproperty PRETTYPRINT_BLOCK_IF_ONLY=TRUE

delete order3
create order3 $PIPS_VALIDDIR/Semantics/order3.f
# create order3 order3.f
module order3
activate PRINT_CODE_PRECONDITIONS
display PRINTED_FILE

close
delete order3
quit
EOF

