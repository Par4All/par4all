
Code after scalarization

int scalarization15(int n)
{
   // BEGIN BLOCK
   int __ld__1;
   int __ld__0;
   int x[100];
   int y[100][100];
   int t[100];
   int i;
   int j;

   for(i = 0; i <= 99; i += 1) {
      // BEGIN BLOCK
      __ld__0 = i;
      for(j = 0; j <= 99; j += 1) {
         // BEGIN BLOCK
         __ld__1 = __ld__0;
         y[i][j] = __ld__0+j;
         // END BLOCK
      }
      // END BLOCK
   }
   return y[n][n]+y[0][0]+y[0][n]+y[n][0];
   // END BLOCK
}
