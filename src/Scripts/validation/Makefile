# $Id$

SCRIPTS = \
	Validate \
	validate-sequential \
	Accept \
	manual_accept \
	bug-to-validate \
	dir-to-validate

INSTALL_UTL =	$(SCRIPTS)

# common stuff
ifdef PIPS_ROOT
ROOT    = $(PIPS_ROOT)
else
ROOT    = ../../..
endif

PROJECT = pips
include $(ROOT)/makes/main.mk
