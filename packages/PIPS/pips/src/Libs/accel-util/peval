#!/bin/sh
# called as peval 'poly' 'symbolic var' 'value'
# prints out the result of the evaluation of the polynomial
scilab-cli -nb <<EOF
n=poly(0,'$2');
p=$1;
r=horner(p,$3);
mfprintf(6,"%f\n",r);
exit(0)
EOF
