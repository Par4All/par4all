// To debug the bounding box normalization: similar empty system not
// detected in Semantics-New/Maisonneuve_misc.sub/maisonneuve07-3.c

int main()
{
  int i, b, x;

  if(b<1||x<0) abort();

  if(b+x+2<=0)
    i++;

  return 0;
}
