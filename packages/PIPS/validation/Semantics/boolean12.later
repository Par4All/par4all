A test with a boolean transformed into an integer comparison is not exploited by the precondition analysis, and later by simplify_control. Short term solution, write if(stabilize) instead of if(stabilize==1), which is better no matter what you might say...
