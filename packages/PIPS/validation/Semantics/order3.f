      program order3
c     to check the ordering used to  print preconditions and transformers

      if(i.eq.1) then
         if(i.eq.3) then
            if(i.eq.2) then
               print *, i
            endif
         endif
      endif

      end
