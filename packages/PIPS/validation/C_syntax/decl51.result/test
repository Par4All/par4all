
Parsed printed file for "decl51!"

/* Simplified version of decl24, but harder than decl50. Bug with
   const declaration */
typedef struct _hdl_progress_handler {
   void *const display_data;
} HDL_progress_handler;


void *const next_data;

int i;
