/* Derived from rsp05.c to debug the parser when a function is declared several time (here, filter) */
/* #include <stdio.h> */


int filter();

int res;

int  main()
{
  int i;
  int j;

  j = filtre(i);
}

int  filter(int x)
{
  int y;

  y = x;

  return res;
}
