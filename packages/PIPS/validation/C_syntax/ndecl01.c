/* Purpose: debug the new version of the parser which keep a statement
   for each declaration. */

void ndecl01(char *dir)
{
  /* One variable declared alone */
  int i;
  /* Two variables declared together */
  int j, k;
  /* A C continue statement */
  ;
  /* An assignment*/
  i = 1;
}
