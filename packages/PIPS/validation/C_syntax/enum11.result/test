/* Check the prettyprint of enum declarations: reference to enum or
   definition of enum

   additional nesting
 */

void enum11();

void enum11()
{
   struct {
      union {enum {gaga} f3;} u;
   } s;
}
