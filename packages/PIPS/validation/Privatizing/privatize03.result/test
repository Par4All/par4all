void pas_vous()
{
   int i, j, k;
   for(i = 0; i <= k-1; i += 1)
      for(j = 12; j <= i-1; j += 1)
         printf("%d", j);
}
extern void pas_vous();
void pas_vous()
{
   int i, j, k;
#pragma omp for private(i,j)
   for(i = 0; i <= k-1; i += 1)
#pragma omp for private(j)
      for(j = 12; j <= i-1; j += 1)
         printf("%d", j);
}
extern void pas_vous();
