#
# Initial code for module pain
#
void pain(char *T)
{
holy:   sain(T);
}
#
# Code after isolate_statement for module pain
#
void pain(char *T)
{
   //PIPS generated variable
   char (*T0)[2] = (char (*)[2]) 0;
   P4A_accel_malloc((void **) &T0, sizeof(char)*2);

holy:   sain(*T0);
   P4A_copy_from_accel_1d(sizeof(char), 2, 2, 0, &T[0], *T0);
   P4A_accel_free(T0);
}
