#
# Initial code for module pain
#
void pain()
{
   dcomplex *t = malloc(sizeof(*t)*18);
holy:   sain(t);
}
#
# Code after isolate_statement for module pain
#
void pain()
{
   dcomplex *t = malloc(sizeof(*t)*18);
   {
      //PIPS generated variable
      dcomplex **t0 = (dcomplex **) 0;
      P4A_accel_malloc((void **) &t0, sizeof(dcomplex *)*1);
      P4A_copy_to_accel_1d(sizeof(dcomplex *), &t, *t0);

holy:      sain(*t0);
      P4A_copy_from_accel_1d(sizeof(t[1]), 1, 1, &t, *t0);
      P4A_accel_free(t0);
   }
}
