void doiv(int a[128][128])
{
   int i, j;
here:
   for(j = 0; j <= 2*N-1; j += 1)
      for(i = 0; i <= N-1; i += 1)
         a[i][j] = 1;
}
void doiv(int a[128][128])
{
   int i, j;
   N = 4;
here:
   for(j = 0; j <= 2*N-1; j += 1)
      for(i = 0; i <= N-1; i += 1)
         a[i][j] = 1;
}

