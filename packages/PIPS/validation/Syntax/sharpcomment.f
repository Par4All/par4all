# let us say a # can start a comment line
# so as to deal with cpp output directly...
      program sharpcomment

# here is some output 
! and a more usual comment
      print *, 'a sharp can start a comment '
      print *, 'for most Fortran compilers'

# last comment.
      end
