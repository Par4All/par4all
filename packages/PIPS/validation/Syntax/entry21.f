C     Check that the formal parameter lists are correct when formal parameter names
C     are identical but in different order

      subroutine entry21(k, m)
      print *, n

      return

      entry increment(m, k)

      m = k + 1

      end
