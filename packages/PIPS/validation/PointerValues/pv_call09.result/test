int foo(int a)
{

// Pointer values: none

   return a;
}
int main()
{

// Pointer values: none

   int a, *p;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   p = &a;

// Pointer values:
// p == &a (exact);

   a = foo(2);

// Pointer values:
// p == &a (exact);

   return 0;
}
