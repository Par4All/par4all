
// IN Pointer values: none


// OUT Pointer values: none

int main()
{

// Pointer values: none

   int a[10];

// Pointer values: none

   int *p;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   int b = 0, c = 1;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   if (b==c)

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

      p = &a[0];
   else

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

      p = &a[1];

// Pointer values:
// p == &a[*] (may);

   p++;

// Pointer values:
// p == &a[*] (may);

   ++p;

// Pointer values:
// p == &a[*] (may);

   p--;

// Pointer values:
// p == &a[*] (may);

   --p;

// Pointer values:
// p == &a[*] (may);

   return 0;
}
