
// IN Pointer values: none


// OUT Pointer values: none

int main()
{

// Pointer values: none

   int a[10];

// Pointer values: none

   int *p;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   int i, j;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   i = 3;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   j = 2;

// Pointer values:
// p == *ANY_MODULE*:*UNDEFINED* (exact);

   p = &a[0];

// Pointer values:
// p == &a[0] (exact);

   p = p+i;

// Pointer values:
// p == &a[*] (may);

   p = p+j;

// Pointer values:
// p == &a[*] (may);

   p = p-i;

// Pointer values:
// p == &a[*] (may);

   p = p-j;

// Pointer values:
// p == &a[*] (may);

   return 0;
}
