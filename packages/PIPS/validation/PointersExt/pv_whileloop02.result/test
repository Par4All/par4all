//  {}
int main()
{
//  points to = {}
   int i = 0;
//  points to = {}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   p = (int *) malloc(10*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-)}

   while (i<10) {
//  points to = {}
      p[i] = i;
//  points to = {}
      i++;
   }
//  {(p,*HEAP*_l_7,-Exact-)}
   return 0;
}
