//  {}
int main()
{
//  points to = {}
   int i = 0;
//  points to = {}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int a[10];
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   do {
//  points to = {}
      p = &a[i];
//  points to = {}
      i++;
   }
   while (i<10);
//  {(p,a[i],-Exact-)}
   return 0;
}
