//  {}
int main()
{
//  points to = {}
   int a[10];
//  points to = {}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   p = &a[0];
//  {(p,a[0],-Exact-)}
   p++;
//  {(p,*ANY_MODULE*:*ANYWHERE*_b0,-Exact-)}
   ++p;
//  {(p,*ANY_MODULE*:*ANYWHERE*_b0,-Exact-)}
   p--;
//  {(p,*ANY_MODULE*:*ANYWHERE*_b0,-Exact-)}
   --p;
//  {(p,*ANY_MODULE*:*ANYWHERE*_b0,-Exact-)}
   return 0;
}
