//  {}
int main()
{
//  points to = {}
   int *a[2];
//  points to = {}
   int b = 0;
//  points to = {}
   int c = 1;
//  points to = {}
   a[0] = &b;
//  {(a[0],b,-Exact-)}
   a[1] = &b;
//  {(a[1],b,-Exact-);(a[0],b,-Exact-)}
   a[1] = &c;
//  {(a[1],c,-Exact-);(a[0],b,-Exact-)}
   return 0;
}
