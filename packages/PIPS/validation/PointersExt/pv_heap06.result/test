//  {}
int main()
{
//  points to = {}
   int *p, *q;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-);(q,
//   *ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   int a = 1;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-);(q,
//   *ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   p = (int *) malloc(2*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-);(q,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   q = (int *) malloc(2*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-);(q,*HEAP*_l_8,-Exact-)}
   free(a==0?p:q);
//  {(p,*HEAP*_l_7,-Exact-);(q,*HEAP*_l_8,-Exact-)}
   return 0;
}
