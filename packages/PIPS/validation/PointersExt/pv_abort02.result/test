//  {}
int main()
{
//  {(a,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int **a;
//  {(a,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int b;
//  {(a,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   a = (int **) NULL;
//  {(a,*NULL_POINTER*,-Exact-)}
   a[0] = &b;
//  {(*NULL_POINTER*,b,-Exact-);(a,*NULL_POINTER*,-Exact-)}
   return 0;
}
