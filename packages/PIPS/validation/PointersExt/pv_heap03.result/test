//  {}
int main()
{
//  points to = {}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int a;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   p = (int *) malloc(2*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-)}
   p[0] = 0;
//  {(p,*HEAP*_l_7,-Exact-)}
   p[1] = 1;
//  {(p,*HEAP*_l_7,-Exact-)}
   free(p);
//  {(p,*HEAP*_l_7,-Exact-)}
   return 0;
}
