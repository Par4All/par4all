int main()
{
   /* P_0 is a pointer to an array. Should be initialized just after, but
        it is not the interest point here */
   float (*P_0)[128][128];

   p4a_kernel_launcher_0(*P_0);

   return 0;
}
void p4a_kernel_launcher_0(float imageout[16384])
{
   float f;
   f = imageout[130];
}
void p4a_kernel_launcher_0(float imageout[16384]);

int main();
/*
 * file for pointer_array_array_to_pointer01.c
 */
void p4a_kernel_launcher_0(float imageout[16384]);

int main();
void p4a_kernel_launcher_0(float imageout[16384])
{
   float f;
   f = imageout[130];
}
int main()
{
   /* P_0 is a pointer to an array. Should be initialized just after, but
        it is not the interest point here */
   float (*P_0)[128][128];

   p4a_kernel_launcher_0(*P_0);

   return 0;
}
int main()
{
   /* P_0 is a pointer to an array. Should be initialized just after, but
        it is not the interest point here */
   float (*P_0)[128][128];

   p4a_kernel_launcher_0(*P_0);

   return 0;
}
void p4a_kernel_launcher_0(float *imageout)
{
   float f;
   f = *(imageout+130);
}
void p4a_kernel_launcher_0(float *imageout);

int main();
/*
 * file for pointer_array_array_to_pointer01.c
 */
void p4a_kernel_launcher_0(float *imageout);

int main();
void p4a_kernel_launcher_0(float *imageout)
{
   float f;
   f = *(imageout+130);
}
int main()
{
   /* P_0 is a pointer to an array. Should be initialized just after, but
        it is not the interest point here */
   float (*P_0)[128][128];

   p4a_kernel_launcher_0(*P_0);

   return 0;
}
