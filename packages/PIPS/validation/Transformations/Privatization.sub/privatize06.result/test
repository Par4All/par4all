#
# Code after privatize_module for module erminable
#
int erminable(char isme)
{
   int the, follow, ce, luke;
   follow = the;
   ce;
   while (luke)
      ;

#pragma omp for private(ce,luke,the)
   for(the = 2; the <= 1; the += 1) {
      ce = luke;
      luke = ce;
   }
}
#
# Code after localize_declaration for module erminable
#
int erminable(char isme)
{
   int the, follow, ce, luke;
   follow = the;
   ce;
   while (luke)
      ;

#pragma omp for private(the)
   for(the = 2; the <= 1; the += 1) {
      //PIPS generated variable
      int ce, luke;
      ce = luke;
      luke = ce;
   }
}
