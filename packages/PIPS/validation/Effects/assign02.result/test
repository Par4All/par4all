
Parsed printed file for "assign02"

void assign02()
{
   s_t s;
   int j;

   s = make_s(1, 2.);
   s.entier = 4;
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}

Proper effetcs for "make_s"

s_t make_s(int i, float x)
{
   s_t *p;
   char *malloc(int);
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ p

   p = (s_t *) malloc(sizeof(s_t));
//               <must be read   >: i p
//               <must be written>: p[0][1]
   p->entier = i;
//               <must be read   >: p x
//               <must be written>: p[0][2]
   p->reel = x;
//               <must be read   >: p p[0]
   return *p;
}

Cumulated effetcs for "make_s"

//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_
s_t make_s(int i, float x)
{
   s_t *p;
   char *malloc(int);
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ p

   p = (s_t *) malloc(sizeof(s_t));
//               <must be read   >: i p
//               <must be written>: p[0][1]
   p->entier = i;
//               <must be read   >: p x
//               <must be written>: p[0][2]
   p->reel = x;
//               <must be read   >: p p[0]
   return *p;
}

Proper effetcs for "assign02"

void assign02()
{
   s_t s;
   int j;
//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ s

   s = make_s(1, 2.);
//               <must be written>: s[1]
   s.entier = 4;
//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ j
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}

Cumulated effetcs for "assign02"

//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_
void assign02()
{
   s_t s;
   int j;
//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ s

   s = make_s(1, 2.);
//               <must be written>: s[1]
   s.entier = 4;
//               <may be read    >: TOP-LEVEL:*MEMORY*
//               <may be written >: TOP-LEVEL:*MEMORY*
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ j
   
   /* invalid lvalue in assignment */
   /* make_s(1, 2.).entier = 4; */
   
   /* But this is OK in rhs */
   j = make_s(1, 2.).entier;
}
