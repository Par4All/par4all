
Parsed printed file for "memcpy01"

int main()
{
   char str1[] = "Sample string";
   char str2[40];
   char str3[40];
   char *res = memcpy(str2, str1, strlen(str1)+1);
   memcpy(str3, "copy successful", 16);
   printf("str1: %s\nstr2: %s\nstr3: %s\n", str1, str2, str3);
   printf("res=%s \n", res);
   return 0;
}

Cumulated effetcs for "memcpy01"

//               <may be read    >: *ANY_MODULE*:*ANYWHERE*
//               <may be written >: stdout[0]
//               <must be read   >: __IO_EFFECTS:LUNS[1]
//               <must be written>: __IO_EFFECTS:LUNS[1]
int main()
{
//               <may be written >: str1[*]
   char str1[] = "Sample string";
   char str2[40];
   char str3[40];
//               <must be written>: res
   char *res = memcpy(str2, str1, strlen(str1)+1);
   memcpy(str3, "copy successful", 16);
//               <may be read    >: stdout[0] str1[*] str2[*] str3[*]
//               <may be written >: stdout[0]
//               <must be read   >: stdout __IO_EFFECTS:LUNS[1]
//               <must be written>: __IO_EFFECTS:LUNS[1]
   printf("str1: %s\nstr2: %s\nstr3: %s\n", str1, str2, str3);
//               <may be read    >: *ANY_MODULE*:*ANYWHERE* stdout[0]
//               <may be written >: stdout[0]
//               <must be read   >: stdout __IO_EFFECTS:LUNS[1]
//               <must be written>: __IO_EFFECTS:LUNS[1]
   printf("res=%s \n", res);
   return 0;
}
