
POINTS TO

//  {}
void point_to04()
{
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   struct s {
      int a;
      int b;
      int c;
      int d;
   } c, *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   p = &c;
//  {(p,c,-Exact-)}

   p->d = p->a;
//  {(p,c,-Exact-)}
   p->d = p->a = 4;
}
