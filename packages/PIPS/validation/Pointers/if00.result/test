int main()
{
   // BEGIN BLOCK
   int **x, *x1, x2, *y, y1;
//               <must be written>: x2
   x2 = 0;
//               <must be written>: y1
   y1 = 1;
//               <must be written>: x1
   x1 = &x2;
//               <must be read   >: TOP-LEVEL:true

   if (true) {
      // BEGIN BLOCK
//               <must be written>: x

      x = &x1;
//               <must be written>: y
      y = &y1;
      // END BLOCK
   }
   else
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: x
      *x = &y;

   return 0;
   // END BLOCK
}
POINTS TO
//  {}
int main()
{
//  points to = {}
   // BEGIN BLOCK
//  points to = {}
   int **x, *x1, x2, *y, y1;
//  points to = {}
   x2 = 0;
//  points to = {}
   y1 = 1;
//  points to = {}
   x1 = &x2;
//  {(x1,x2,-Exact-)}

   if (true) {
//  {(x1,x2,-Exact-)}
      // BEGIN BLOCK
//  {(x1,x2,-Exact-)}

      x = &x1;
//  {(x,x1,-Exact-);(x1,x2,-Exact-)}
      y = &y1;
      // END BLOCK
   }
   else
//  {(x1,x2,-Exact-)}
      *x = &y;
//  {(y,y1,-MAY-);(x,x1,-MAY-);(x1,x2,-Exact-)}

   return 0;
   // END BLOCK
}
