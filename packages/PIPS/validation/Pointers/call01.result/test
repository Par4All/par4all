
Parsed code for "call01"

void call01(int *pi, tf_t *q)
{
   *pi = 1;
   /* pi is passed by value: pi++ does not generate a summary effect
        for call01 */
   pi++;
   q->one = 1;
   (q->two)[4] = 2;
}

Code for "call01"

void call01(int *pi, tf_t *q)
{
   *pi = 1;
   /* pi is passed by value: pi++ does not generate a summary effect
        for call01 */
   pi++;
   q->one = 1;
   (q->two)[4] = 2;
}

Proper effects for "call01"

void call01(int *pi, tf_t *q)
{
//               <must be read   >: pi
//               <must be written>: pi[0]
   *pi = 1;
//               <must be read   >: pi
//               <must be written>: pi
   /* pi is passed by value: pi++ does not generate a summary effect
        for call01 */
   pi++;
//               <must be read   >: q
//               <must be written>: q[0][1]
   q->one = 1;
//               <must be read   >: q
//               <must be written>: q[0][2][4]
   (q->two)[4] = 2;
}

Cumulated effects for "call01"

//               <must be read   >: pi q
//               <must be written>: pi[0] q[0][1] q[0][2][4]
void call01(int *pi, tf_t *q)
{
//               <must be read   >: pi
//               <must be written>: pi[0]
   *pi = 1;
//               <must be read   >: pi
//               <must be written>: pi
   /* pi is passed by value: pi++ does not generate a summary effect
        for call01 */
   pi++;
//               <must be read   >: q
//               <must be written>: q[0][1]
   q->one = 1;
//               <must be read   >: q
//               <must be written>: q[0][2][4]
   (q->two)[4] = 2;
}

Proper effects for "main"

int main()
{
   int i;
   tf_t s;
//               <must be written>: i s[1] s[2][4]

   call01(&i, &s);
}

POINTS TO

//  {(pi,_pi_1,-Exact-)}
void call01(int *pi, tf_t *q)
{
//  {(pi,_pi_1,-Exact-)}
   *pi = 1;
//  {(pi,_pi_1,-Exact-)}
   /* pi is passed by value: pi++ does not generate a summary effect
        for call01 */
   pi++;
//  {(pi,*ANYWHERE*,-MAY-);(pi,_pi_1,-Exact-)}
   q->one = 1;
//  {(pi,*ANYWHERE*,-MAY-);(pi,_pi_1,-Exact-)}
   (q->two)[4] = 2;
}

POINTS TO

//  {}
int main()
{
//  points to = {}
   int i;
//  points to = {}
   tf_t s;
//  points to = {}

   call01(&i, &s);
}
