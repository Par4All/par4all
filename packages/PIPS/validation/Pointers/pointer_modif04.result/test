
Proper effects for "init_s"

void init_s(s_t s, int max)
{
   // BEGIN BLOCK
//             <    is read   >: max _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: s.tab _MALLOC_EFFECTS:_MALLOC_

   s.tab = (float *) malloc(max*sizeof(float));
//             <    is read   >: max
//             <    is written>: s.max
   s.max = max;
   // END BLOCK
}

POINTS TO

//  {}
void init_s(s_t s, int max)
{
//  points to = {}
   // BEGIN BLOCK
//  points to = {}

   s.tab = (float *) malloc(max*sizeof(float));
//  {(s[tab],*ANY_MODULE*:*HEAP*,-Exact-)}
   s.max = max;
   // END BLOCK
}

POINTS TO

//  {}
int main()
{
//  points to = {}
   // BEGIN BLOCK
//  points to = {}
   s_t s;
//  {(s[tab],*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   int j;
//  {(s[tab],*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}

   init_s(s, 10);
//  {(s[tab],*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}

   compute_s(s, 10);
//  {(s[tab],*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   
   
   return 0;
   // END BLOCK
}
