
Pointer proper effects

int main()
{
   // BEGIN BLOCK
   int i;
   int *p;
   int **q;
//             <    is read   >: p
//             <    is written>: p[0]

   *p = 1;
//             <    is read   >: q
//             <    is written>: q[0]
   *q = &i;

   return 0;
   // END BLOCK
}

Points-to

//  {}
int main()
{
//  points to = {}
   // BEGIN BLOCK
//  points to = {}
   int i;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-);(q,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}
   int **q;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-);(q,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}

   *p = 1;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-);(q,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}
   *q = &i;
//  {(*ANY_MODULE*:*ANYWHERE*,i,-Exact-);(p,*ANY_MODULE*:*NOWHERE*,
//   -Exact-);(q,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   return 0;
   // END BLOCK
}
