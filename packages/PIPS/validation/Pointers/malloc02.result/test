
POINTS TO

//  {}
int main(int argc, char *argv[])
{
//  {(pi,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int *pi;
//  {(pi,*ANY_MODULE*:*NOWHERE*,-Exact-);(px,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}
   float *px;
//  {(pi,*ANY_MODULE*:*NOWHERE*,-Exact-);(px,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}

   pi = (int *) malloc(sizeof(int));
//  {(pi,*ANY_MODULE*:*HEAP*_b0,-Exact-);(px,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}
   px = (float *) malloc(sizeof(int));
//  {(pi,*ANY_MODULE*:*HEAP*_b0,-Exact-);(px,*ANY_MODULE*:*HEAP*_b1,
//   -Exact-)}

   return 0;
}
