
PROPER  EFFECTS

int main(int argc, char *argv[])
{
   int t;
//             <    is written>: t
   
   
   for(t = 0; t <= 63; t += 1)
//             <may be written>: tab[*][*]
      compute();
   
   
   return 0;
}

POINTS-TO

//  {(argv[*],_argv_1[0],-Exact-)}
int main(int argc, char *argv[])
{
//  {(argv[*],_argv_1[0],-Exact-)}
   int t;
//  {(argv[*],_argv_1[0],-Exact-)}
   
   
   for(t = 0; t <= 63; t += 1)
//  {(argv[*],_argv_1[0],-Exact-)}
      compute();
//  {(argv[*],_argv_1[0],-Exact-)}
   
   
   return 0;
}
