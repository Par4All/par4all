
POINTS TO

C  {}
int main(int argc, char *argv[])
{
//  points to = {}
   int *pi;
//  points to = {}
   float *px;
//  points to = {}
   float *pz;
//  points to = {}
   int (*pt)[30];
//  points to = {}

   pi = (int *) malloc(sizeof(int));
//  {(pi,*HEAP*_l_13,-MAY-)}
   px = (float *) malloc(sizeof(float));
//  {(pi,*HEAP*_l_13,-MAY-);(px,*HEAP*_l_14,-MAY-)}
   pz = (float *) malloc(sizeof(float));
//  {(pi,*HEAP*_l_13,-MAY-);(px,*HEAP*_l_14,-MAY-);(pz,*HEAP*_l_15,
//   -MAY-)}
   pt = (int (*)[30]) malloc(sizeof(*pt));
//  {(pi,*HEAP*_l_13,-MAY-);(pt,*HEAP*_l_16,-MAY-);(px,*HEAP*_l_14,
//   -MAY-);(pz,*HEAP*_l_15,-MAY-)}

   return 0;
}
