
Pointer proper effects

int main()
{
   // BEGIN BLOCK
   typedef struct my_struct {
      int i;
      int *p;
   } my_str;
   my_str *m, *n;
   int j;
//               <must be written>: j
   j = 2;
//               <must be read   >: _MALLOC_EFFECTS:_MALLOC_
//               <must be written>: _MALLOC_EFFECTS:_MALLOC_ m
   m = (my_str *) malloc(sizeof(my_str));
//               <must be read   >: m
//               <must be written>: m[0][1]
   m->i = 1;
//               <must be read   >: m
//               <must be written>: m[0][2]
   m->p = &j;
//               <must be read   >: m
//               <must be written>: n
   n = m;

   return 0;
   // END BLOCK
}

Points-to

//  {}
int main()
{
//  points to = {}
   // BEGIN BLOCK
//  points to = {}
   typedef struct my_struct {
      int i;
      int *p;
   } my_str;
//  points to = {}
   my_str *m, *n;
//  points to = {}
   int j;
//  points to = {}
   j = 2;
//  points to = {}
   m = (my_str *) malloc(sizeof(my_str));
//  {(m,*ANY_MODULE*:*HEAP*_b0,-MAY-)}
   m->i = 1;
//  {(m,*ANY_MODULE*:*HEAP*_b0,-MAY-)}
   m->p = &j;
//  {(m[0][2],j,-Exact-)}
   n = m;
//  {(m[0][2],j,-Exact-);(n,j,-Exact-)}

   return 0;
   // END BLOCK
}
