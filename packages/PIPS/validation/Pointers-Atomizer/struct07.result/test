atomized code
void struct07()
{
   extern complex imagein[256][256], imageout[256][256];
   int i, j, k, l;
   float z1, z2;
   float x[256][256];

   for(k = 0; k <= 255; k += 1)
      imageout[k][1].re = z1;

   for(i = 0; i <= 255; i += 1)
      for(j = 0; j <= 255; j += 1)
         x[i][j] = 0.;
}
