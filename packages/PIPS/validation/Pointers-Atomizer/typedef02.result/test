atomized code
int main(int argc, char *argv[])
{
   int t, i;
   //PIPS generated variable
   float F_0;

   for(i = 1; i <= 62; i += 1) {
      F_0 = 64*sizeof(float);
      tab3[i] = malloc(F_0);
   }
   for(t = 0; t <= 63; t += 1)
      compute();

   for(i = 1; i <= 62; i += 1)
      free(tab3[i]);

   return 0;
}
