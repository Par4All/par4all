int main()
{
// (0,1)
//:   BEGIN UNSTRUCTURED
   {
// (1,1)
      // BEGIN BLOCK
   {{
// (1,2)
      // BEGIN BLOCK
   {{
// (1,3)
      // BEGIN BLOCK
   {{
// (1,4)
      double a, b;
// (1,5)

      b = 1;
// (1,6)
      a = (b*b);
   }}
      // END BLOCK
      {
// (1,7)
         // BEGIN BLOCK
      {{
// (1,8)
         int i;
// (1,9)
         
         /* First loop */
         for (i = 0;i<67;i++)
// (1,10)
            a += b;
      }}
         // END BLOCK
      }
   }}
      // END BLOCK
      {
// (1,11)
         // BEGIN BLOCK
      {{
// (1,12)
         int i;
// (1,13)
         
         // Second loop same index
         for (i = 32;i>(-34);i--)
// (1,14)
            b -= a;
      }}
         // END BLOCK
      }
// (1,15)
      
      /* after the loop */
      return 0;
   }}
      // END BLOCK
   }
//:   END UNSTRUCTURED
}
