// Goal: check constraint simplification by function
// small_positive_slope_reduce_coefficients_with_bounding_box()

// Second half of slope07.c

#include <assert.h>

void slope09()
{
  int x, y, k;

  assert(0<=y && y<=9);

  if(61*y<=55*x+54) {
    k = x + y;
  }
  else {
    k = x + y;
  }

  return;
}
