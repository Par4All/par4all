
! initial function

freia_status freia_15(freia_data2d *o, freia_data2d *i, int32_t *r0, int32_t *r1, int32_t *r2, int32_t c)
{
   // test re ordering of mes
   freia_aipo_add_const(o, i, c);                                       /*0007*/
   freia_aipo_global_min(i, r0);                                        /*0008*/
   freia_aipo_global_max(i, r1);                                        /*0009*/
   freia_aipo_global_vol(i, r2);                                        /*0010*/
   return FREIA_OK;                                                     /*0011*/
}

! graphs

// graph for dag "dag_0" of module "freia_15" in dot format
digraph "dag_0" {
  // inputs
  "i" [shape=circle];

  // outputs
  "o" [shape=circle];

  // computation vertices
  "11 ?" [shape=none];
  "10 vol" [shape=diamond];
  "9 max" [shape=diamond];
  "8 min" [shape=diamond];
  "7 +." [shape=trapezium,orientation=270];
  "7 +." -> "o";
  "i" -> "10 vol";
  "i" -> "9 max";
  "i" -> "8 min";
  "i" -> "7 +.";
}
// graph for dag "dag_cleaned_0" of module "freia_15" in dot format
digraph "dag_cleaned_0" {
  // inputs
  "i" [shape=circle];

  // outputs
  "o" [shape=circle];

  // computation vertices
  "11 ?" [shape=none];
  "10 vol" [shape=diamond];
  "9 max" [shape=diamond];
  "8 min" [shape=diamond];
  "7 +." [shape=trapezium,orientation=270];
  "7 +." -> "o";
  "i" -> "10 vol";
  "i" -> "9 max";
  "i" -> "8 min";
  "i" -> "7 +.";
}
// graph for dag "freia_15_helper_0_0" of module "freia_15" in dot format
digraph "freia_15_helper_0_0" {
  // inputs
  "i" [shape=circle];

  // outputs
  "o" [shape=circle];

  // computation vertices
  "7 +." [shape=trapezium,orientation=270];
  "7 +." -> "o";
  "10 vol" [shape=diamond];
  "9 max" [shape=diamond];
  "8 min" [shape=diamond];
  "i" -> "7 +.";
  "i" -> "10 vol";
  "i" -> "9 max";
  "i" -> "8 min";
}

! acceleration function

#include <freiaCommon.h>
#include <freiaCoarseGrain.h>
#include <spoc.h>

// FREIA-SPoC helper function for module freia_15
freia_status freia_15_helper_0_0_0(freia_data2d * o0, freia_data2d * i0, int32_t * red0, int32_t * red1, int32_t * red2, int32_t cst3)
{
  spoc_instr si;
  spoc_param sp;
  spoc_reduction reduc;
  freia_microcode mcode;
  freia_dynamic_param dynparam;
  freia_reduction_results redres;
  freia_op_sel op = 0;
  freia_op_param param;
  freia_status ret;
  int i;

  // init pipe to nop
  spoc_init_pipe(&si, &sp, 16);


  // STAGE 0
  // i [0 input 0] -> [0 measure 0] 0 
  si.mux[0][0].op = SPOC_MUX_IN0;

  // i [0 measure 0] -> [0 measure 0] 0 
  // nope

  // i [0 measure 0] -> [0 measure 0] 0 
  // nope


  // STAGE 1
  // i [0 measure 0] -> [1 alu] 7 add_const
  // i [1 input 0] -> [1 alu] 7 add_const
  // nope
  // alu 7 add_const stage 1
  si.alu[1][0].op = SPOC_ALU_ADD_IN0_CONST;
  si.alu[1][0].constant = cst3;


  // output image o on 0
  // o [1 alu] -> [1 output 0] 0 null
  si.mux[1][0].op = SPOC_MUX_IN1;
  si.mux[1][2].op = SPOC_MUX_IN0;

  mcode.raw = (freia_ptr) &si;
  mcode.size = sizeof(spoc_instr);

  dynparam.raw = (freia_ptr) &sp;
  dynparam.size = sizeof(spoc_param);

  redres.raw = (freia_ptr) &reduc;
  redres.size = sizeof(spoc_reduction);

  ret = freia_cg_write_microcode(&mcode);
  ret |= freia_cg_write_dynamic_param(&dynparam);

  // actual call of spoc hardware
  freia_cg_template_process_2i_2o(op, param, o0, NULL, i0, NULL);

  // get reductions
  freia_cg_read_reduction_results(&redres);

  // measure 8 global_min stage 0 side 0
  *red0 = (int32_t) reduc.measure[0][0].minimum;

  // measure 9 global_max stage 0 side 0
  *red1 = (int32_t) reduc.measure[0][0].maximum;

  // measure 10 global_vol stage 0 side 0
  *red2 = (int32_t) reduc.measure[0][0].volume;

  return ret;
}

! updated function

freia_status freia_15(freia_data2d *o, freia_data2d *i, int32_t *r0, int32_t *r1, int32_t *r2, int32_t c)
{
   // test re ordering of mes
   freia_15_helper_0_0_0(o, i, r0, r1, r2, c);                          /*0007*/
   return FREIA_OK;                                                     /*0011*/
}
