
! initial function

freia_status freia_16(freia_data2d *o0, freia_data2d *o1, freia_data2d *i, int32_t c, int32_t *k, int32_t *r0, int32_t *r1, int32_t *r2)
{
   // test re ordering of mes
   freia_aipo_add_const(o0, i, c);                                      /*0007*/
   freia_aipo_erode_6c(o1, i, k);                                       /*0008*/
   freia_aipo_global_min(i, r0);                                        /*0009*/
   freia_aipo_global_max(i, r1);                                        /*0010*/
   freia_aipo_global_vol(i, r2);                                        /*0011*/
   return FREIA_OK;                                                     /*0012*/
}

! accelerated function

// graph for dag "dag_0" of module "freia_16" in dot format
digraph "dag_0" {
  // inputs
  "i" [shape=box];

  // outputs
  "o0" [shape=box];
  "o1" [shape=box];

  // computation vertices
  "11 global_vol" [shape=circle];
  "i" -> "11 global_vol";
  "10 global_max" [shape=circle];
  "i" -> "10 global_max";
  "9 global_min" [shape=circle];
  "i" -> "9 global_min";
  "8 erode_6c" [shape=circle];
  "i" -> "8 erode_6c";
  "8 erode_6c" -> "o1";
  "7 add_const" [shape=circle];
  "i" -> "7 add_const";
  "7 add_const" -> "o0";
}
// graph for dag "freia_16_helper_0" of module "freia_16" in dot format
digraph "freia_16_helper_0" {
  // inputs
  "i" [shape=box];

  // outputs
  "o1" [shape=box];
  "o0" [shape=box];

  // computation vertices
  "7 add_const" [shape=circle];
  "i" -> "7 add_const";
  "7 add_const" -> "o0";
  "8 erode_6c" [shape=circle];
  "i" -> "8 erode_6c";
  "8 erode_6c" -> "o1";
  "11 global_vol" [shape=circle];
  "i" -> "11 global_vol";
  "10 global_max" [shape=circle];
  "i" -> "10 global_max";
  "9 global_min" [shape=circle];
  "i" -> "9 global_min";
}
#include <freiaCommon.h>
#include <freiaCoarseGrain.h>
#include <spoc.h>

// FREIA-SPoC helper function for module freia_16
freia_status freia_16_helper_0(freia_data2d * o0, freia_data2d * o1, freia_data2d * i0, int32_t * red0, int32_t * red1, int32_t * red2, int32_t *kern3, int32_t cst4)
{
  spoc_instr si;
  spoc_param sp;
  spoc_reduction reduc;
  freia_microcode mcode;
  freia_dynamic_param dynparam;
  freia_reduction_results redres;
  freia_op_sel op = 0;
  freia_op_param param;
  freia_status ret;
  int i;

  // init pipe to nop
  spoc_init_pipe(&si, &sp, 16);


  // STAGE 0
  // i [0 input 0] -> [0 measure 0] i
  si.mux[0][0].op = SPOC_MUX_IN0;

  // i [0 measure 0] -> [0 measure 0] i
  // nope

  // i [0 measure 0] -> [0 measure 0] i
  // nope


  // STAGE 1
  // i [0 measure 0] -> [1 poc 0] o1
  // i [1 input 0] -> [1 poc 0] o1
  // nope
  si.poc[1][0].op = SPOC_POC_ERODE;
  si.poc[1][0].grid = SPOC_POC_6_CONNEX;
  for(i=0 ; i<9 ; i++)
    sp.poc[1][0].kernel[i] = kern3[i];

  // i [1 input 0] -> [1 alu] o0
  // nope
  si.alu[1][0].op = SPOC_ALU_ADD_IN0_CONST;
  si.alu[1][0].constant = cst4;


  // output image o1 on 0 and image o0 on 1
  // o1 [1 poc 0] -> [1 output 0] o1
  si.mux[1][0].op = SPOC_MUX_IN0;
  // o0 [1 alu] -> [1 output 1] o0
  si.mux[1][1].op = SPOC_MUX_IN0;
  si.mux[1][3].op = SPOC_MUX_IN0;

  mcode.raw = (freia_ptr) &si;
  mcode.size = sizeof(spoc_instr);

  dynparam.raw = (freia_ptr) &sp;
  dynparam.size = sizeof(spoc_param);

  redres.raw = (freia_ptr) &reduc;
  redres.size = sizeof(spoc_reduction);

  ret = freia_cg_write_microcode(&mcode);
  ret |= freia_cg_write_dynamic_param(&dynparam);

  // actual call of spoc hardware
  freia_cg_template_process_2i_2o(op, param, o0, o1, i0, NULL);

  // get reductions
  freia_cg_read_reduction_results(&redres);

  // get reduction results
  *red0 = (int32_t) reduc.measure[0][0].minimum;

  // get reduction results
  *red1 = (int32_t) reduc.measure[0][0].maximum;

  // get reduction results
  *red2 = (int32_t) reduc.measure[0][0].volume;

  return ret;
}

! new function

freia_status freia_16(freia_data2d *o0, freia_data2d *o1, freia_data2d *i, int32_t c, int32_t *k, int32_t *r0, int32_t *r1, int32_t *r2)
{
   // test re ordering of mes
   freia_16_helper_0(o1, o0, i, r0, r1, r2, k, c);                      /*0007*/
   return FREIA_OK;                                                     /*0012*/
}
