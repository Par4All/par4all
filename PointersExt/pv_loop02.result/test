//  {}
int main()
{
//  points to = {}
   int i;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   p = (int *) malloc(10*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-)}

   for(i = 0; i <= 9; i += 1)
//  {(p,*HEAP*_l_7,-Exact-)}
      p[i] = i;
//  {(p,*HEAP*_l_7,-Exact-)}
   return 0;
}
