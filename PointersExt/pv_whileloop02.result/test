//  {}
int main()
{
//  points to = {}
   int i = 0;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   p = (int *) malloc(10*sizeof(int));
//  {(p,*HEAP*_l_7,-Exact-)}

   while (i<10) {
//  {(p,*HEAP*_l_7,-Exact-)}
      p[i] = i;
//  {(p,*HEAP*_l_7,-Exact-)}
      i++;
   }
//  {(p,*HEAP*_l_7,-Exact-)}
   return 0;
}
