//  {}
int main()
{
//  points to = {}
   int **a;
//  {(a,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int b;
//  {(a,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   a[0] = &b;
//  {(*ANY_MODULE*:*ANYWHERE*_b0,b,-Exact-);(a,*ANY_MODULE*:*NOWHERE*,
//   -Exact-)}
   return 0;
}
