//  {}
int main()
{
//  points to = {}
   int a[10];
//  points to = {}
   int *p;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   int i, j;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   i = 3;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   j = 2;
//  {(p,*ANY_MODULE*:*NOWHERE*_b0,-Exact-)}
   p = &a[0];
//  {(p,a[0],-Exact-)}
   p = p+i;
//  {(p,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   p = p+j;
//  {(p,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   p = p-i;
//  {(p,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   p = p-j;
//  {(p,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   return 0;
}
