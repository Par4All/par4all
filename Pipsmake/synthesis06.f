      program synthesis06

C     Check for non-trivial symbolic constants

      integer lload
      integer nbr
      parameter (nbr = 2)
      parameter  (lload  = 3*nbr )
 
      call  hidenh ( lhiden, lload, 9999)

      end
