#
# Initial code for module pain
#
void pain()
{
   char *t = malloc(10);
holy:   sain(t);
}
#
# Code after isolate_statement for module pain
#
void pain()
{
   char *t = malloc(10);
   {
      //PIPS generated variable
      char **t0 = (char **) 0;
      P4A_accel_malloc((void **) &t0, sizeof(char *)*1);
      P4A_copy_to_accel_1d(sizeof(char *), &t, *t0);

holy:      sain(*t0);
      P4A_copy_from_accel_1d(sizeof(char), 1, 2, &t, *t0);
      P4A_accel_free(t0);
   }
}
