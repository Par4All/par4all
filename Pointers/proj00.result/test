
POINTS TO


// Points To IN: none


// Points To OUT: none

int main(void)
{

// Points To: none

   int i;

// Points To: none

   int **pp;

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

   srand(time((void *) 0));

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT


   if (alea()) {

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

      int *pi = &i;

// Points To:
// pi -> i , EXACT
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

      pp = &pi;

// Points To:
// pi -> i , EXACT
// pp -> pi , EXACT


      **pp = 1;
   }

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , MAY
// pp -> *ANY_MODULE*:*NOWHERE*_b1 , MAY

   
   // pp points to a dead value in the stack
   printf("%p\n", pp);

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , MAY
// pp -> *ANY_MODULE*:*NOWHERE*_b1 , MAY

   
   // possible segfault.
   // should generate an error while analyzing?
   **pp = 0;

// Points To:
// pp -> *ANY_MODULE*:*NOWHERE*_b0 , MAY
// pp -> *ANY_MODULE*:*NOWHERE*_b1 , MAY

   return 0;
}

// Points To IN: none


// Points To OUT: none

static int alea(void)
{

// Points To: none

   int r = rand();

// Points To: none

   printf("%d\n", r);

// Points To: none

   return rand()%2==1;
}

Warnings

INTRAPROCEDURAL_POINTS_TO_ANALYSIS[main] (user_call_condition_to_points_to) Interprocedural points-to not implemented yet. Call site ignored. 0x25b6a80 0x2420270 0x1.
INTRAPROCEDURAL_POINTS_TO_ANALYSIS[main] (points_to_block_projection) Dangling pointer pp 
INTRAPROCEDURAL_POINTS_TO_ANALYSIS[main] (user_call_condition_to_points_to) Interprocedural points-to not implemented yet. Call site ignored. 0x25b6a80 0x24214e0 (nil).
