
POINTS TO

C  {}
s_t make_s(int i, float x)
{
   // BEGIN BLOCK
   s_t *p;

   p = (s_t *) malloc(sizeof(s_t));
//  {(p,*ANYWHERE*,-MAY-)}
   p->entier = i;
//  {(p,*ANYWHERE*,-MAY-)}
   p->reel = x;
//  {(p,*ANYWHERE*,-MAY-)}
   return *p;
   // END BLOCK
}

POINTS TO

