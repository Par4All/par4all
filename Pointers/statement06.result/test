int main()
{
   // BEGIN BLOCK
   int a, b, c, d;
   int *pa, *pb, *pc, *pd;
   int **ppa, **ppb, **ppc, **ppd;
//               <must be written>: a
   a = 1;
//               <must be written>: b
   b = 2;
//               <must be written>: c
   c = 3;
//               <must be written>: d
   d = 4;
//               <must be written>: pa

   pa = &a;
//               <must be written>: ppa
   ppa = &b;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: ppa
   *ppa = &pa;
//               <must be written>: pb

   pb = &b;
//               <must be written>: ppb
   ppb = &a;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: ppb
   *ppb = &pb;
//               <must be written>: pc

   pc = &c;
//               <must be written>: ppc
   ppc = &d;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: ppc
   *ppc = &pc;
//               <must be written>: pd

   pd = &d;
//               <must be written>: ppd
   ppd = &c;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: ppd
   *ppd = &pd;
//               <must be read   >: pd
//               <must be written>: pc

   pc = pd;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: ppc

   *ppc = &a;
//               <may be written >: *ANY_MODULE*:*ANYWHERE*
//               <must be read   >: pc ppa

   *ppa = pc;
//               <may be read    >: *ANY_MODULE*:*ANYWHERE*
//               <must be written>: pa

   pa = *ppd;
//               <may be read    >: *ANY_MODULE*:*ANYWHERE*
//               <may be written >: *ANY_MODULE*:*ANYWHERE*

   *ppa = *ppd;

   return 0;
   // END BLOCK
}
POINTS TO
//  {}
int main()
{
   // BEGIN BLOCK
   int a, b, c, d;
   int *pa, *pb, *pc, *pd;
   int **ppa, **ppb, **ppc, **ppd;
   a = 1;
   b = 2;
   c = 3;
   d = 4;

   pa = &a;
//  {(pa,a,-Exact-)}
   ppa = &b;
//  {(pa,a,-Exact-);(ppa,b,-Exact-)}
   *ppa = &pa;
//  {(pa,a,-Exact-);(ppa,b,-Exact-)}

   pb = &b;
//  {(pb,b,-Exact-);(pa,a,-Exact-);(ppa,b,-Exact-)}
   ppb = &a;
//  {(pb,b,-Exact-);(pa,a,-Exact-);(ppa,b,-Exact-);(ppb,a,-Exact-)}
   *ppb = &pb;
//  {(pb,b,-Exact-);(pa,a,-Exact-);(ppa,b,-Exact-);(ppb,a,-Exact-)}

   pc = &c;
//  {(pc,c,-Exact-);(pb,b,-Exact-);(pa,a,-Exact-);(ppb,a,
//   -Exact-);(ppa,b,-Exact-)}
   ppc = &d;
//  {(pc,c,-Exact-);(pb,b,-Exact-);(pa,a,-Exact-);(ppa,b,
//   -Exact-);(ppb,a,-Exact-);(ppc,d,-Exact-)}
   *ppc = &pc;
//  {(pc,c,-Exact-);(pb,b,-Exact-);(pa,a,-Exact-);(ppa,b,
//   -Exact-);(ppb,a,-Exact-);(ppc,d,-Exact-)}

   pd = &d;
//  {(pd,d,-Exact-);(pc,c,-Exact-);(pb,b,-Exact-);(pa,a,-Exact-);(ppc,
//   d,-Exact-);(ppb,a,-Exact-);(ppa,b,-Exact-)}
   ppd = &c;
//  {(pa,a,-Exact-);(ppb,a,-Exact-);(ppc,d,-Exact-);(pd,d,
//   -Exact-);(pc,c,-Exact-);(ppa,b,-Exact-);(ppd,c,-Exact-);(pb,b,
//   -Exact-)}
   *ppd = &pd;
//  {(pa,a,-Exact-);(ppc,d,-Exact-);(ppd,c,-Exact-);(pd,d,
//   -Exact-);(pc,c,-Exact-);(ppa,b,-Exact-);(ppb,a,-Exact-);(pb,b,
//   -Exact-)}

   pc = pd;
//  {(pb,b,-Exact-);(ppb,a,-Exact-);(ppd,c,-Exact-);(pa,a,
//   -Exact-);(pd,d,-Exact-);(ppa,b,-Exact-);(ppc,d,-Exact-);(pc,d,
//   -Exact-)}

   *ppc = &a;
//  {(pb,b,-Exact-);(ppd,c,-Exact-);(ppc,d,-Exact-);(pa,a,
//   -Exact-);(pd,d,-Exact-);(ppa,b,-Exact-);(ppb,a,-Exact-);(pc,d,
//   -Exact-)}

   *ppa = pc;
//  {(pa,a,-Exact-);(b,d,-Exact-);(ppc,d,-Exact-);(ppb,a,-Exact-);(pd,
//   d,-Exact-);(pc,d,-Exact-);(ppa,b,-Exact-);(ppd,c,-Exact-);(pb,b,
//   -Exact-)}

   pa = *ppd;
//  {(pb,b,-Exact-);(ppd,c,-Exact-);(ppb,a,-Exact-);(b,d,-Exact-);(pd,
//   d,-Exact-);(ppa,b,-Exact-);(ppc,d,-Exact-);(pc,d,-Exact-)}

   *ppa = *ppd;
//  {(pc,d,-Exact-);(ppc,d,-Exact-);(ppb,a,-Exact-);(pb,b,-Exact-);(b,
//   d,-Exact-);(ppa,b,-Exact-);(ppd,c,-Exact-);(pd,d,-Exact-)}

   return 0;
   // END BLOCK
}
