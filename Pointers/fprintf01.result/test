
POINTS TO

//  {}
int main()
{
//  {(pFile,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   FILE *pFile;
//  {(pFile,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   int n;
//  {(pFile,*ANY_MODULE*:*NOWHERE*,-Exact-)}
   char name[100];
//  {(pFile,*ANY_MODULE*:*NOWHERE*,-Exact-)}

   pFile = fopen("myfile.txt", "w");
//  {(pFile,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   for(n = 0; n <= 2; n += 1) {
//  {(pFile,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
      puts("please, enter a name: ");
//  {(pFile,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
      gets(name);
//  {(pFile,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
      fprintf(pFile, "Name %d %s\n", n, name);
   }
//  {(pFile,*ANY_MODULE*:*ANYWHERE*,-Exact-)}
   fclose(pFile);
}
