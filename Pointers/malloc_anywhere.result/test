POINTS TO
C  {}
int main()
{
//  points to = {}
   typedef struct {
      int *p;
   } my_str;

//  points to = {}
   my_str *s, *s1;
//  points to = {}
   int i = 0;
//  points to = {}
   s = (my_str *) malloc(sizeof(my_str));
//  {(s,*ANYWHERE*,-MAY-)}
   s1 = (my_str) malloc(sizeof(my_str));
//  {(s1,*ANYWHERE*,-MAY-);(s,*ANYWHERE*,-MAY-)}
   return 0;
}
