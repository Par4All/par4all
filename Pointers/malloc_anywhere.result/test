POINTS TO
C  {}
int main(int argc, char *argv[])
{
   // BEGIN BLOCK
   int i, j;
   int *a[64];

   for (i = 0;i<64;i++) {
      // BEGIN BLOCK
      a[i] = malloc(64*sizeof(float));
      for (j = 0;j<64;j++)
         a[i][j] = 0.;
      // END BLOCK
   }

   return 0;
   // END BLOCK
}
