
Proper effects for "dependence05"

int * dependence05()
{
   int *a;
   int *b;
   int *c;
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: b _MALLOC_EFFECTS:_MALLOC_

   b = (int *) malloc(10*sizeof(int));
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: c _MALLOC_EFFECTS:_MALLOC_
   c = (int *) malloc(10*sizeof(int));
//             <    is read   >: b
//             <    is written>: a

   a = b;
//             <may be written>: *ANY_MODULE*:*ANYWHERE*
//             <    is read   >: a
   // a and b are aliased, a points_to b[0]
   *a = 0;
//             <    is read   >: c
//             <    is written>: a
   // write effect on *a may be visible from caller, because b escape
   
   a = c;
//             <may be written>: *ANY_MODULE*:*ANYWHERE*
//             <    is read   >: a
   // a and b are aliased, a points_to b[0]
   *a = 0;
//             <    is read   >: c _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_
   // write effect won't be visible from caller, because c doesn't escape
   
   free(c);
//             <    is read   >: b

   return b;
}

POINTS TO for "dependence05"


// Points To IN: none


// Points To OUT:
// dependence05 -> *HEAP*_l_10[0] , EXACT

int * dependence05()
{

// Points To: none

   int *a;

// Points To:
// a -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

   int *b;

// Points To:
// a -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT
// b -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

   int *c;

// Points To:
// a -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT
// b -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT
// c -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT


   b = (int *) malloc(10*sizeof(int));

// Points To:
// a -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT

   c = (int *) malloc(10*sizeof(int));

// Points To:
// a -> *ANY_MODULE*:*NOWHERE*_b0 , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *HEAP*_l_11[0] , EXACT


   a = b;

// Points To:
// a -> *HEAP*_l_10[0] , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *HEAP*_l_11[0] , EXACT

   // a and b are aliased, a points_to b[0]
   *a = 0;

// Points To:
// a -> *HEAP*_l_10[0] , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *HEAP*_l_11[0] , EXACT

   // write effect on *a may be visible from caller, because b escape
   
   a = c;

// Points To:
// a -> *HEAP*_l_11[0] , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *HEAP*_l_11[0] , EXACT

   // a and b are aliased, a points_to b[0]
   *a = 0;

// Points To:
// a -> *HEAP*_l_11[0] , EXACT
// b -> *HEAP*_l_10[0] , EXACT
// c -> *HEAP*_l_11[0] , EXACT

   // write effect won't be visible from caller, because c doesn't escape
   
   free(c);

// Points To:
// b -> *HEAP*_l_10[0] , EXACT
// c -> *ANY_MODULE*:*NOWHERE* , EXACT


   return b;
}

