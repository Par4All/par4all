
POINTS TO for exit01


// Points To IN: none


// Points To OUT: unreachable

int * exit01(int *p)
{

// Points To: none


   p = (int *) malloc(4*sizeof(int));

// Points To:
// p -> *ANY_MODULE*:*HEAP**ANYWHERE* , MAY

   exit(0);

// Points To: unreachable

   return p;
}

// Points To IN: none


// Points To OUT: unreachable

int main()
{

// Points To: none

   int *q, *r;

// Points To:
// q -> *ANY_MODULE*:*NOWHERE* , EXACT
// r -> *ANY_MODULE*:*NOWHERE* , EXACT

   q = exit01(r);

// Points To: unreachable

   return 0;
}

PROPER  EFFECTS WITH POINTS TO

int * exit01(int *p)
{
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: p _MALLOC_EFFECTS:_MALLOC_

   p = (int *) malloc(4*sizeof(int));
   exit(0);
   return p;
}
int main()
{
   int *q, *r;
//             <    is read   >: r _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: q _MALLOC_EFFECTS:_MALLOC_
   q = exit01(r);
   return 0;
}

CUMULATED  EFFECTS WITH POINTS TO

//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_
int * exit01(int *p)
{
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: p _MALLOC_EFFECTS:_MALLOC_

   p = (int *) malloc(4*sizeof(int));
   exit(0);
   return p;
}
//             <    is read   >: _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: _MALLOC_EFFECTS:_MALLOC_
int main()
{
   int *q, *r;
//             <    is read   >: r _MALLOC_EFFECTS:_MALLOC_
//             <    is written>: q _MALLOC_EFFECTS:_MALLOC_
   q = exit01(r);
   return 0;
}

POINTS TO for exit01


// Points To IN: none


// Points To OUT: unreachable

int * exit01(int *p)
{

// Points To: none


   p = (int *) malloc(4*sizeof(int));

// Points To:
// p -> *ANY_MODULE*:*HEAP**ANYWHERE* , MAY

   exit(0);

// Points To: unreachable

   return p;
}

// Points To IN: none


// Points To OUT: unreachable

int main()
{

// Points To: none

   int *q, *r;

// Points To:
// q -> *ANY_MODULE*:*NOWHERE* , EXACT
// r -> *ANY_MODULE*:*NOWHERE* , EXACT

   q = exit01(r);

// Points To: unreachable

   return 0;
}
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (user_call_to_points_to_interprocedural) Function has no side effect on its formal context via pointer variables.
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (pointer_assignment_to_points_to) Expression "exit01(r)" could not be dereferenced at line 14.
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (user_call_to_points_to_interprocedural) Function has no side effect on its formal context via pointer variables.
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (pointer_assignment_to_points_to) Expression "exit01(r)" could not be dereferenced at line 14.
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (user_call_to_points_to_interprocedural) Function has no side effect on its formal context via pointer variables.
INTERPROCEDURAL_POINTS_TO_ANALYSIS[main] (pointer_assignment_to_points_to) Expression "exit01(r)" could not be dereferenced at line 14.
unknown[unknown] (activate_phase) Rule `INTERPROCEDURAL_POINTS_TO_ANALYSIS' already active
