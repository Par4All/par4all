# 1
main()
{
  struct key
  {
    int node;
  };

  struct key1
  {
    int node;
  };

  union key2
  {
    int node;
  };

  struct node
  {
    float node;
  };

  int key;
  int key2;
  int node;
}
# 1 "$VDIR/struct_member_name.c"
# 1 "<built-in>"
# 1 "<command line>"
# 1 "$VDIR/struct_member_name.c"
extern main();
