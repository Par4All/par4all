# 1
main()
{
  int i = 1;
  switch (i)
    {
    case 1:
      printf("1 = %d",i);
      break;
    case 2:
      printf("2 = %d",i);
      break;
    default:
    }
}
# 1 "$VDIR/switch.c"
# 1 "<built-in>"
# 1 "<command line>"
# 1 "$VDIR/switch.c"
extern main();
